{
  "name": "css-reset-udyux",
  "version": "1.0.2",
  "description": "My evolving CSS reset, drawing some inspiration from reset.css and normalize among other projects.",
  "author": "Nicolas Udy <dev@udy.io>",
  "license": "MIT",
  "main": "reset.min.css",
  "files": [
    "reset.{css,scss}"
  ],
  "scripts": {
    "publish": "node publish",
    "prepack": "stylelint ./*.{css,scss} --fix",
    "postversion": "git push --follow-tags"
  },
  "devDependencies": {
    "prettier": "^1.16.2",
    "stylelint": "^9.10.1",
    "stylelint-config-udyux": "^1.2.4"
  }
}
